# Fellbacher Wines E-Commerce Platform

Этот проект представляет собой современную E-Commerce платформу для винодельни Fellbacher, построенную с использованием передовых технологий веб-разработки на базе **Next.js 15**. Приложение сочетает в себе высокую производительность, строгую типизацию и премиальный дизайн с фокусом на пользовательский опыт.

---

## 🛠 Технологический Стек

Мы используем современный стек технологий для обеспечения масштабируемости, безопасности и отзывчивости интерфейса:

### Core Frameworks
- **Next.js 15 (App Router)**: Использование серверных компонентов (React Server Components) для мгновенной загрузки, оптимизации SEO и мощной системы маршрутизации.
- **React 19**: Новейшая версия библиотеки для эффективного рендеринга и управления UI.
- **TypeScript**: Полная типизация всего проекта для исключения ошибок на этапе разработки и упрощения рефакторинга.

### Styling & UI
- **Tailwind CSS 4**: Быстрая и гибкая стилизация с использованием современных CSS-функций.
- **Framer Motion**: Продвинутые анимации для микро-взаимодействий, переходов страниц и оживления интерфейса.
- **Lucide React**: Оптимизированный набор векторных иконок.
- **HeroUI (NextUI)**: Библиотека высококачественных компонентов для обеспечения единообразия дизайна.

### State Management & Logic
- **Context API**: Глобальное управление состоянием через специализированные провайдеры (Cart, Wishlist, Auth, Wines, Orders).
- **i18n (Custom Logic)**: Легковесная и быстрая система интернационализации с поддержкой DE/EN и динамическими параметрами.
- **WooCommerce API**: Интеграция с бэкендом WordPress через защищенные API-маршруты для получения актуальных данных о товарах.

---

## 📂 Структура Проекта

Проект следует четкому разделению ответственности: маршрутизация в `app/`, логика в `lib/`, а UI-компоненты в `components/`.

```
fellbacher-wines/
├── app/                        # Next.js Routing Layer (App Router)
│   ├── api/                    # Serverless API маршруты (например, прокси для WooCommerce)
│   ├── aboutUs/                # Маршруты страниц "О компании"
│   ├── dashboard/              # Личный кабинет пользователя
│   ├── events/                 # Управление мероприятиями и бронированием
│   ├── loyalty/                # Бонусная программа
│   ├── shop/                   # Основной каталог и детальные страницы вин (/shop/[wineId])
│   ├── layout.tsx              # Корневой макет
│   ├── providers.tsx           # Агрегатор всех Context-провайдеров
│   └── page.tsx                # Главная страница (Hero, Featured)
├── components/                 # UI Library (Вынесено из app/ для чистоты)
│   ├── layout/                 # Глобальные элементы: Header, Footer, Logo, Navigation
│   ├── wine/                   # Компоненты каталога: WineCard, Filters, Stats
│   ├── login/                  # Аутентификация: AuthModal, LoginForm
│   ├── contact/                # Обратная связь и формы
│   └── (others)                # Специализированные компоненты модулей
├── lib/                        # Доменная логика и утилиты
│   ├── contexts/               # Глобальное состояние: Auth, Cart, Wines, Wishlist, Orders
│   ├── data/                   # Моковые данные и статические справочники (wines.ts, events.ts)
│   ├── types/                  # Централизованные TypeScript интерфейсы (Wine, Event, User)
│   ├── constants/              # Глобальные константы: контакты, настройки API
│   ├── i18n.tsx                # Механизм локализации
│   └── woocommerce.ts          # Клиент для взаимодействия с WordPress/WooCommerce
├── public/                     # Статические медиа-файлы и изображения
└── config files                # tailwind, tsconfig, next, postcss и др.
```

---

## 🚀 Подробное описание ключевых модулей

### 1. Система Управления Состоянием (Contexts)
В `/lib/contexts` сосредоточена вся бизнес-логика приложения:
- **`AuthContext`**: Управляет сессией пользователя, сохраняет данные в `localStorage` и обеспечивает защиту приватных маршрутов.
- **`CartContext`**: Сложная логика корзины: расчет итогов, валидация количеств и сохранение состояния между сессиями.
- **`WinesContext`**: "Умный" поставщик данных. Пытается загрузить реальные вина через API, но при ошибке или пустом ответе автоматически переключается на моковые данные, гарантируя работоспособность фронтенда.

### 2. Каталог и Магазин (`/shop`)
- **Динамическая маршрутизация**: Использование `[wineId]` для создания уникальных SEO-оптимизированных страниц товаров.
- **Фильтрация и поиск**: Интегрированная система фильтрации по типам вина и полнотекстовый поиск, работающий в реальном времени.

### 3. Модуль Мероприятий (`/events`)
- **Лендинги событий**: Индивидуальные страницы для каждого типа мероприятия (Kellerblicke, Weinproben и др.) с уникальным контентом.
- **Booking Flow**: Многошаговая форма бронирования с валидацией авторизации перед записью.

### 4. Интернационализация (i18n)
- Поддержка мгновенного переключения языков (DE/EN) без перезагрузки страниц.
- Система переводов поддерживает вложенные параметры (например, `{count} results`), обеспечивая гибкость текстов.

### 5. Progressive Web App (PWA)
- **Мобильный опыт**: Приложение полностью адаптировано для iOS и Android. Поддерживает установку на главный экран, имеет заставку (splash screen) и офлайн-кэширование через Service Worker.
- **Инструкция**: В мобильном браузере нажмите "Поделиться" (iOS) или меню Chrome (Android) и выберите "Add to Home Screen".

### 6. Система Skeletons (HeroUI)
- **Загрузка данных**: Вместо пустых экранов или спиннеров используются компоненты Skeleton, которые визуально имитируют структуру карточек вин, деталей и событий.

### 7. Мобильный UX (Bottom Navigation)
- Для удобства управления одной рукой на смартфонах реализована нижняя навигация (BottomNav), в то время как на десктопах остается стандартный Header.


### 5. Безопасность и API
- Ключи WooCommerce API хранятся на стороне сервера в `.env.local`.
- Запросы от фронтенда всегда идут через защищенные маршруты в `app/api/`, что исключает утечку Consumer Secret.

---

© 2024 Fellbacher Wines. Разработано с фокусом на качество и инновации.
