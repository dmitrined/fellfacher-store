# Fellbacher Wines E-Commerce Platform

Этот проект представляет собой современную E-Commerce платформу для винодельни Fellbacher, построенную с использованием передовых технологий веб-разработки на базе **Next.js 15**. Приложение сочетает в себе высокую производительность, строгую типизацию и премиальный дизайн с фокусом на пользовательский опыт.

---

## 🛠 Технологический Стек

Мы используем современный стек технологий для обеспечения масштабируемости, безопасности и отзывчивости интерфейса:

### Core Frameworks
- **Next.js 16 (App Router)**: Использование серверных компонентов (React Server Components) для мгновенной загрузки, оптимизации SEO и мощной системы маршрутизации.
- **React 19**: Новейшая версия библиотеки для эффективного рендеринга и управления UI.
- **TypeScript**: Полная типизация всего проекта для исключения ошибок на этапе разработки и упрощения рефакторинга.

### Styling & UI
- **Tailwind CSS 4**: Быстрая и гибкая стилизация с использованием современных CSS-функций.
- **Framer Motion**: Продвинутые анимации для микро-взаимодействий, переходов страниц и оживления интерфейса.
- **Lucide React**: Оптимизированный набор векторных иконок.
- **HeroUI (NextUI)**: Библиотека высококачественных компонентов для обеспечения единообразия дизайна.

### State Management & Logic
- **Context API**: Глобальное управление состоянием через специализированные провайдеры (Cart, Wishlist, Auth, Wines, Orders, UI).
- **i18n (Custom Logic)**: Легковесная и быстрая система интернационализации с поддержкой DE/EN и динамическими параметрами.
- **WooCommerce API**: Интеграция с бэкендом WordPress через защищенные API-маршруты для получения актуальных данных о товарах.

---

## 📂 Структура Проекта

Проект следует четкому разделению ответственности: маршрутизация в `app/`, логика в `lib/`, а UI-компоненты в `components/`.

```text
fellbacher-wines/
├── app/                            # Маршрутизация (App Router)
│   ├── (aboutUs)/                  # Информация о компании
│   │   ├── whoWeAre/page.tsx       # О нас
│   │   ├── ourTeam/page.tsx        # Команда
│   │   └── nextGeneration/page.tsx # Молодое поколение Winzer
│   ├── api/                        # Serverless функции
│   │   └── wines/route.ts          # Proxy для WooCommerce API
│   ├── dashboard/                  # Личный кабинет
│   ├── events/                     # Календарь и события
│   │   ├── kellerblicke/page.tsx   # Экскурсии в погреба
│   │   ├── [id]/book/page.tsx       # Бронирование мероприятия
│   │   └── ...                     # Другие категории (Weinproben, Afterwork и т.д.)
│   ├── loyalty/                    # Программа лояльности
│   │   └── qr-code/page.tsx        # Персональный QR-код клиента
│   ├── shop/                       # Каталог вин
│   │   ├── [wineId]/page.tsx       # Карточка вина
│   │   └── page.tsx                # Главная страница магазина
│   ├── globals.css                 # Глобальные стили (Tailwind 4)
│   ├── layout.tsx                  # Корневой шаблон (Header, Footer, BottomNav)
│   └── providers.tsx               # Глобальная обертка провайдеров
├── components/                     # Повторно используемые UI-компоненты
│   ├── layout/                     # Основная обвязка
│   │   ├── Header.tsx              # Десктопная шапка
│   │   ├── BottomNav.tsx           # Мобильная навигация (Phase 6)
│   │   ├── Footer.tsx              # Подвал
│   │   └── Header/                 # Суб-компоненты шапки (MobileMenu, Cart, etc.)
│   ├── ui/                         # Атомарные UI элементы
│   │   └── Skeletons/              # Загрузочные состояния (Card, Detail, Event)
│   ├── wine/                       # Логика магазина (Card, Filters, Stats)
│   └── login/                      # Модальные окна входа и регистрации
├── lib/                            # Бизнес-логика
│   ├── contexts/                   # Глобальное состояние (Cart, Auth, UI, etc.)
│   ├── constants/                  # Конфигурации и навигационные данные
│   ├── data/                       # Статические справочники (Fallback данные)
│   ├── types/                      # TypeScript интерфейсы
│   ├── i18n.tsx                    # Система переводов
│   └── woocommerce.ts              # Инструментарий для связи с WordPress
├── public/                         # PWA иконки, манифест и статика
├── next.config.ts                  # Конфигурация Next.js (Turbo, Images)
└── tailwind.config.js              # Настройки стилей
```

---

## 🚀 Подробное описание ключевых модулей

### 1. Система Управления Состоянием (Contexts)
В `/lib/contexts` сосредоточена вся бизнес-логика приложения:
- **`AuthContext`**: Управляет сессией пользователя и защитой приватных маршрутов.
- **`CartContext`**: Расчет корзины, валидация количеств и сохранение стейта.
- **`UIContext`**: Глобальное управление видимостью мобильного меню и других элементов интерфейса.
- **`WinesContext`**: "Умный" поставщик данных. Синхронизируется с WooCommerce API, автоматически переключаясь на Fallback-данные при сбоях.

### 2. Мобильная Эргономика (Phase 6)
- **Bottom Navigation**: Для удобства управления одной рукой на смартфонах реализована нижняя панель (Home, Shop, Events, MyCode, More).
- **Mobile Menu**: Глобальное меню (Mehr), вынесенное в корень `layout.tsx`, обеспечивающее мгновенный отклик.

### 3. Progressive Web App (PWA)
- Приложение полностью адаптировано для установки на смартфоны.
- Включает `manifest.json`, иконки разных разрешений и оптимизированные Viewport-настройки для ощущения нативного приложения.

---

© 2024-2025 Fellbacher Wines. Разработано с фокусом на качество и инновации.

